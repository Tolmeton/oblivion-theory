# F-1 統一表 v0.2 修正 proposal (G-3 混成路線)

作成: 2026-04-24
作成者: Claude (Advisor)
対象: `03_忘却論｜Oblivion/drafts/standalone/統一表の関手化_構想ドラフト_v0.2.md`
行: L359, L363-L371, §7 Problem 6 (L1578-L1598)

---

## §0 方針宣言 (G-3)

G-3 方針:

1. **`d: C^∞ → C^∞/ℝ`** は **Paper VI §2.3 L99-L100** を grounding として明示 (Paper II 直接参照を外す) [SOURCE: 論文VI_行為可能性は忘却である_草稿.md L95-L102]
2. **`w = e^{-kα}`** と **`dw/dα = -kw`** は **本稿 (統一表 v0.2) の self-contained 前提**として明示 [SOURCE: `_p6_intermediate/Paper2_full_scan.md` 結論 (B) / L474-L480]
3. **Paper II 本体への書き戻し**は本 proposal の scope 外 (Tolmetes 判断で後続可能)
4. **Phase 3 Lemma C** は Paper II 依存から self-contained 前提への橋に再定義

---

## §1 診断

### 1.1 現状 (統一表 v0.2 の該当箇所)

**L359 (複素化射 ω の定義)** [SOURCE: 統一表 v0.2 L359]:

> Paper II の忘却関手 d の固有関数 w(f,α) = e^{-kα} と、Paper I の方向定理が与える角度 θ(f,α) を合成した複素化射 ω(f,α) := w(f,α)·e^{iθ(f,α)} を導入する。

**L363-L371 (e の位置)** [SOURCE: 統一表 v0.2 L363-L371]:

> **e の位置 — Paper II を使うと最小核が指数になる**:
>
> Paper II では、微分 d は積分定数を落とす忘却関手として定式化される: d: C^∞ → C^∞/ℝ（商写像）。各 α ステップでの射の変化 dw は、忘却操作 d の無限小適用である。
>
> 方程式 dw/dα = −kw の解 w = e^{−kα} が「自然な底」である根拠: **d(e^x)/dx = e^x**。忘却操作 d を適用しても形が変わらない — これは d の固有関数。「どの α でも同じに見える」スケール不変な忘却の形は e^{−kα} だけ。
>
> > **e は便利だから置いた記号ではない。Paper II の忘却関手 d を連続半群の生成子として読むなら、指数核が最小候補になる。**
>
> ただし、この「一意」は忘却進行が連続であり、局所生成則 `dw/dα=-kw` を持つことを仮定したときに限る。したがって本節で必要なのは、`e` の既成事実化ではなく、この連続半群構造をどこで公理化するかである。

### 1.2 grounding 状態の確定 (F-2 結果より)

| claim | grounding 状態 |
|:---|:---|
| `d: C^∞ → C^∞/ℝ` (商写像) | **Paper VI §2.3 L99-L100 に grounded** [SOURCE: 論文VI_草稿.md L95-L102] |
| `dw/dα = -kw` (局所生成則) | **Class A なし** — Paper II 本体では未観測、F3 sap 精読台帳に二次記述のみ [SOURCE: `_p6_intermediate/grounding_hunt.md` L494] |
| `w(f,α) = e^{-kα}` (指数固有関数) | **Class A なし** — 同上 [SOURCE: `_p6_intermediate/grounding_hunt.md` L494-L495] |
| `k ↔ -logφ·Δα^{-1}` | **heuristic correspondence** 明示 [SOURCE: 統一表 v0.2 L1549, L1582; `_p6_intermediate/grounding_hunt.md` L497] |

### 1.3 hidden TAINT chain (F-2 で発見)

```
統一表 v0.2 L359, L363-L371
   └─ Paper II の忘却関手 d の固有関数 w(f,α)=e^{-kα} を claim
        └─ Paper II §1.1 が grounding と想定
             └─ Paper VI §2.3 L97 が「Paper II §1.1 は 2026-03-29 修正で精密化」と主張
                  └─ Paper II 本体 (2026-04 時点 1805 行) には該当記述が未実装
                      [SOURCE: `_p6_intermediate/Paper2_full_scan.md` L474-L480]
```

→ Paper II 本体修正は本 proposal の scope 外。Paper VI §2.3 の既存記述を grounding として受け入れる。

---

## §2 修正文案

### 2.1 L359 の修正案

#### Before

```markdown
**複素化射 ω — Paper I ↔ Paper II の接続**:

Paper II の忘却関手 d の固有関数 w(f,α) = e^{-kα} と、Paper I の方向定理が与える角度 θ(f,α) を合成した複素化射 ω(f,α) := w(f,α)·e^{iθ(f,α)} を導入する。この射の虚部 Im(ω) = w·sin(θ) が方向定理の力 F_ij に対応する。e は w の関数形（忘却関手 d の固有関数）、i は θ = π/2 での位相（90° 回転作用素）、π は θ の非整列サイクルの半周期として、ω の定義から自然に読み出される。
```

#### After

```markdown
**複素化射 ω — Paper I ↔ 商写像 d の接続**:

本稿の複素化射 ω の構成には、以下の二つの前提を置く:

1. **商写像としての d** [grounded]: 忘却関手 d は `d: C^∞ ↠ C^∞/ℝ` という商写像 (ker(d) = ℝ) として定式化される [SOURCE: 論文VI_行為可能性は忘却である_草稿.md §2.3 L95-L102]
2. **d の指数固有関数** [self-contained premise]: d を連続半群の生成子として読むとき、局所生成則 `dw/dα = -kw` の解 `w(f,α) = e^{-kα}` を固有関数として採用する (§5.2 で詳述)

これらの前提の下で、Paper I の方向定理が与える角度 θ(f,α) を合成した複素化射 ω(f,α) := w(f,α)·e^{iθ(f,α)} を導入する。この射の虚部 Im(ω) = w·sin(θ) が方向定理の力 F_ij に対応する。e は w の関数形、i は θ = π/2 での位相（90° 回転作用素）、π は θ の非整列サイクルの半周期として、ω の定義から自然に読み出される。
```

### 2.2 L363-L371 の修正案

#### Before

```markdown
**e の位置 — Paper II を使うと最小核が指数になる**:

Paper II では、微分 d は積分定数を落とす忘却関手として定式化される: d: C^∞ → C^∞/ℝ（商写像）。各 α ステップでの射の変化 dw は、忘却操作 d の無限小適用である。

方程式 dw/dα = −kw の解 w = e^{−kα} が「自然な底」である根拠: **d(e^x)/dx = e^x**。忘却操作 d を適用しても形が変わらない — これは d の固有関数。「どの α でも同じに見える」スケール不変な忘却の形は e^{−kα} だけ。

> **e は便利だから置いた記号ではない。Paper II の忘却関手 d を連続半群の生成子として読むなら、指数核が最小候補になる。**

ただし、この「一意」は忘却進行が連続であり、局所生成則 `dw/dα=-kw` を持つことを仮定したときに限る。したがって本節で必要なのは、`e` の既成事実化ではなく、この連続半群構造をどこで公理化するかである。
```

#### After

```markdown
**e の位置 — 商写像 d を半群生成子として読めば最小核が指数になる**:

微分 d は積分定数を落とす忘却関手として、Paper VI §2.3 [SOURCE: 論文VI_行為可能性は忘却である_草稿.md L95-L102] で `d: C^∞ ↠ C^∞/ℝ` (商写像、ker(d) = ℝ) と定式化される。本稿はこれを基盤に、各 α ステップでの射の変化 dw を d の無限小適用として読む。

**前提 P5.2 (self-contained)**: 連続半群生成子として d を読むとき、その固有関数は `w(f,α) = e^{-kα}`、局所生成則は `dw/dα = -kw` である。

根拠: `d(e^x)/dx = e^x` — 忘却操作 d を適用しても形が変わらない。「どの α でも同じに見える」スケール不変な忘却の形は e^{-kα} のみ。

> **e は便利だから置いた記号ではない。商写像 d を連続半群の生成子として読むなら、指数核が最小候補になる。**

**grounding status (2026-04-24)**:

- `d: C^∞ → C^∞/ℝ` の商写像構造は Paper VI §2.3 L99-L100 に grounded [SOURCE: 論文VI_行為可能性は忘却である_草稿.md L95-L102]
- 固有関数前提 `w = e^{-kα}` と `dw/dα = -kw` は Paper II 本体 (2026-04 時点 1805 行) では未観測 [SOURCE: `03_忘却論｜Oblivion/calculations/_p6_intermediate/Paper2_full_scan.md` 結論 (B), L474-L480]
- したがって P5.2 は本稿の self-contained 前提として扱い、Paper II 本体への書き戻し (§1.1 の正式修正) は本稿の scope 外とする
- 連続半群構造の厳密な公理化と、離散排他系 (§5 Problem 3 補題 1) との接続は Problem 6 Lemma A-D の責務 (§7)

この「一意」は忘却進行が連続であり、局所生成則 `dw/dα = -kw` を持つことを仮定したときに限る。
```

### 2.3 §7 Problem 6 への注記追加 (L1585 付近)

#### Before (L1578-L1598 の Known セクションのみ)

```markdown
6. **Problem 6 — 連続極限での φ の位置づけ** [優先課題]:

   **Known (SOURCE).** Paper II の忘却関手 d の固有関数 $w(f,\alpha) = e^{-k\alpha}$ は一つの射の重みを記述する。問題 3 の補題 1 は、排他サイクルの有効配置数が $L(n) = \mathrm{Tr}(T^n) \sim \varphi^n$ に従うことを与える。さらに Paper 0 §2.2a と Paper XIV §8 は、離散的公理計数が連続極限で微分幾何へ溶け、$\varphi$ が直接には見えなくなることを明示している。
```

#### After

```markdown
6. **Problem 6 — 連続極限での φ の位置づけ** [優先課題]:

   **Known (SOURCE).** 本稿 §5.2 前提 P5.2 の `w(f,\alpha) = e^{-k\alpha}` は一つの射の重みを記述する。問題 3 の補題 1 は、排他サイクルの有効配置数が $L(n) = \mathrm{Tr}(T^n) \sim \varphi^n$ に従うことを与える。さらに Paper 0 §2.2a と Paper XIV §8 は、離散的公理計数が連続極限で微分幾何へ溶け、$\varphi$ が直接には見えなくなることを明示している。

   **注 (grounding status, 2026-04-24)**: Problem 6 で用いる `w = e^{-k\alpha}` 系の grounding 状態:
   - `d: C^\infty \to C^\infty/\mathbb{R}` → Paper VI §2.3 L99-L100 に grounded [SOURCE: 論文VI_行為可能性は忘却である_草稿.md L95-L102]
   - `w(f,\alpha) = e^{-k\alpha}` と `dw/d\alpha = -kw` → Paper II 本体では未観測 [SOURCE: `_p6_intermediate/Paper2_full_scan.md` 結論 (B)]。本稿 §5.2 前提 P5.2 として self-contained に定義
   - `k \leftrightarrow -\log\varphi \cdot \Delta\alpha^{-1}` → heuristic correspondence のみ。厳密化は Lemma C の責任
```

---

## §3 Phase 3 Lemma C への含意

### 3.1 Lemma C の目標の書き換え

修正前 (暗黙): 「Lemma B で得た密度量が、**Paper II の一射重み** $w(f,\alpha)=e^{-k\alpha}$ を生む有効作用または生成関数の減衰率 $k$ に写る」

修正後 (明示): 「Lemma B で得た密度量 `κ(λ)` が、**本稿 §5.2 前提 P5.2 の一射重み** `w(f,α) = e^{-kα}` の減衰率 `k` に、`k = κ(1)` として接続する」

### 3.2 Phase 3 の scope 明確化

- **Phase 3 の仕事**: 本稿の self-contained 前提 P5.2 と、Lemma B の `κ(λ)` 正規化を、`k = κ(1)` として接続する自己整合性の証明
- **Phase 3 の scope 外**: Paper II 本体の正式修正 (2026-03-29 予定修正の本実装)
- **Phase 3 で open になるもの**: `d: C^∞ → C^∞/ℝ` の連続半群としての厳密な公理化 (Paper VI §2.3 の商写像構造 + 半群生成子としての読み替え)

### 3.3 self-contained 化の含意

Phase 3 Lemma C は Paper II への書き戻しを要求しなくなるため、Codex 実行時の blocker が軽減される。Lemma A (coarse-graining 関手) + Lemma B (λ-族正規化) + Lemma C (k = κ(1) 接続) の 3 段階が本稿 v0.2 内部で閉じる。

---

## §4 追加タスク (Tolmetes 判断で実行可否)

### T1. Paper II 本体の正式修正 (G-1 相当、本 proposal の scope 外)

Paper VI §2.3 L97 が「2026-03-29 修正で精密化した」と主張する内容を、実際に Paper II §1.1 に反映する。

- 対象: `03_忘却論｜Oblivion/drafts/series/論文II_相補性は忘却である_草稿.md` §1.1
- 追加内容: `d: C^∞ → C^∞/ℝ` の商写像定式化、ker(d) = ℝ の意義
- 工数: 数時間 (Paper II の spine に影響しない追加のみ)
- 効果: Paper VI および統一表 v0.2 の参照が grounded に戻る

### T2. 固有関数 `w = e^{-kα}` の Paper VI への追加

Paper VI §2.3 に「商写像 d を半群生成子として読めば固有関数は `e^{-kα}`」という Lemma を追加し、統一表 v0.2 の参照先を Paper VI に一本化する。

- 対象: `論文VI_行為可能性は忘却である_草稿.md` §2.3
- 追加内容: 半群生成子読み替え + `e^{-kα}` 固有関数の Lemma (統一表 §5.2 P5.2 の Paper VI 化)
- 工数: 数時間
- 効果: 統一表 v0.2 の self-contained 前提を Paper VI に昇格できる

### T3. Problem 6 の解決後に P5.2 を定理化

Problem 6 Lemma A-D が解けた後、P5.2 は「本稿の前提」から「Problem 6 の系として証明された命題」に昇格できる。これは Phase 5 統合段階のタスク。

---

## §5 実装手順

### 5.1 本 proposal の採択 (Tolmetes 承認後)

1. Claude が Edit tool で統一表 v0.2 L359 を置換 (§2.1 After)
2. Claude が Edit tool で統一表 v0.2 L363-L371 を置換 (§2.2 After)
3. Claude が Edit tool で統一表 v0.2 §7 Problem 6 Known に注記追加 (§2.3 After)
4. 変更箇所の spot check (diff 確認)
5. 統一表 v0.2 の version update (例: v0.2.1 として grounding status 追加)

### 5.2 proposal 却下 / 変更要求時の代替

- G-1 (Paper II 本体修正) を選ぶなら、Paper II §1.1 に追加文案を別途 draft
- G-2 (Paper VI も書き換え) を選ぶなら、Paper VI §2.3 L97 のトーンダウン案を別途 draft

---

## §6 Phase 1 着手への含意

Phase 1 (Lemma A: coarse-graining 関手 G_{Δα}) は本 proposal の採択と**独立**に着手可能。

- Lemma A の収束先 `Φ(α)` は Paper II L1209-L1229 (blanket 遷移) と L1277 (作用汎関数) で grounded [SOURCE: `_p6_intermediate/P6_source_ledger.md`]
- Lemma A では `e^{-kα}` / `dw/dα = -kw` は使用しない
- したがって Phase 1 Codex Brief は現時点の情報で確定可能

---

## §7 要約 (Tolmetes 向け)

1. **修正箇所は 3 つ**: 統一表 v0.2 L359 / L363-L371 / §7 Problem 6 Known
2. **方針は G-3 混成**: `d: C^∞ → C^∞/ℝ` は Paper VI grounded、`w = e^{-kα}` は self-contained
3. **Paper II 本体は変更しない** (本 proposal の scope 外)
4. **Phase 3 Lemma C は self-contained 化で軽量化**
5. **Phase 1 は独立に着手可能**
6. **追加タスク T1-T3 は Tolmetes 判断待ち** (G-3 採択には不要)
7. **承認されれば Claude が Edit tool で実装**

### 決定待ち

- Tolmetes は本 proposal (G-3) を承認するか
- Edit の実行を Claude に委任するか、Tolmetes 自身が行うか
- Phase 1 を proposal 採択後に投入するか、並行で投げるか
