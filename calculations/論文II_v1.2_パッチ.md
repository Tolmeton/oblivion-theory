# Paper II v1.1 → v1.2 パッチ文書
## Face Lemma 修正 + Δd 割り当て基準の統一

**作成日**: 2026-03-27
**対象**: 論文II_相補性は忘却である_草稿.md (v1.1)
**修正箇所**: 8箇所

**後続注記 (2026-04-14)**: v1.2 以後の reader-facing 補助線として、Face Lemma を Hamming / LDPC / n-cell tower の語彙で再読するノート `drafts/incubator/FaceLemma_技術設計.md` を追加した。これは本パッチの厳密化を変更しないが、「なぜ 3射が detectability の最小条件なのか」を説明する補助線になる。

---

## PATCH 1: §2.1 Layer 1 条件に第5条件を追加

### 場所
§2.1 の Layer 1 リストの末尾（条件4の後）

### 追加内容
```
  5. **CPS 経由** (Face Lemma 整合): 全ての非退化 2-simplex $(f, g, h)$ に対し、$f$ と $g$ は CPS スパンの異なる脚 ($U_A$ 方向と $U_B$ 方向) に沿う
```

### 影響
「4条件」→「5条件」に変更が必要な箇所: §2.5 各項の「Layer 1 四条件の検証」→「Layer 1 五条件の検証」。ただし既存の4条件の検証内容は変更不要（第5条件は Face Lemma 具体化の中で暗黙に検証済み）。

---

## PATCH 2: §2.3 Δd 割り当て基準の統一 [精読レポート P2-C1 の修正]

### 問題
現行の Δd 割り当てが「位相多様体 = 0-cell」「計量 = 2-cell (Sym² の 2)」とテンソル階数を cell 次元と混同している。

### 修正方針
Δd を**忘却関手のカーネルの構造次元**として統一的に定義する。

### §2.3 テーブルの差替え

**旧:**
```
| 一般相対論 | Lor | 位相 M / 計量 g | 2 | M = 0-cell (開集合), g ∈ Sym²T* = 2-cell | I | 真空解あり → M が前提 |
| 心身問題 | FEP 圏 | 身体 Θ / 心 ∇^(α) / 意識 F | 2 | Θ = 0-cell, ∇^(α) = 1-cell, F = 2-cell | I | 岩は存在 → 身体が前提 |
```

**新:**
```
| 一般相対論 | Lor | 位相 M / 計量 g | 2 | ker(U_top) = 計量 g (Sym²T* の切断 = 2階構造); ker(U_met) = 位相 (0階構造)。Δd = |2-0| = 2 | I | 真空解あり → M が前提 |
| 心身問題 | FEP 圏 | 身体 Θ / 心 ∇^(α) / 意識 F | 2 | ker(U_body) = 接続+曲率 (1階+2階構造); ker(U_mind) = 座標 (0階構造)。Δd = max(2,1)-0 = 2 | I | 岩は存在 → 身体が前提 |
```

### §2.3 テーブルの直前に追加する注記

```
**Δd の統一的定義 (v1.2).** Δd は忘却関手のカーネルが失う**微分構造の最高階数**の差として定義する:

$$\Delta d := |\text{ord}(\ker U_A) - \text{ord}(\ker U_B)|$$

ここで ord(ker U) は、U が忘却する構造の微分的階数——0階 = 点的データ (関数値, 状態)、1階 = 方向的データ (微分, 接続, 勾配)、2階 = 曲率的データ (2階微分, 曲率テンソル, 計量)——である。この定義は de Rham 複体の次数（k-形式の k）と整合するが、テンソルの「添字の数」(Sym²T* の「2」) とは異なる概念であることに注意: Sym²T* の「2」は対称積の次数であり、計量が 2-形式であることを意味しない。計量は 0-形式ではなく (点的データでない)、かつ 1-形式でもない (方向依存性は持つが、方向自体ではない)。計量は**2つの方向を同時に消費して距離を返す**構造であり、これが ord = 2 の根拠である。
```

---

## PATCH 3: §2.5.3 と §2.5.4 の Δd 根拠の修正

### §2.5.3 (GR) の Δd 根拠

**旧:**
```
**Δd の根拠.** 位相多様体 M は「点の集合 + 隣接関係」（0-cell 的: 開集合による位相構造）。計量 g_{μν} は「2つの接ベクトルを食べてスカラーを返す双線形形式」であり、Sym²T*M の切断として 2-cell 構造を持つ。Einstein 方程式 G_{μν} = 8πGT_{μν} は 2-cell レベルの方程式。真空解 (T_{μν} = 0) の存在が CPS0'（位相は計量なしに存在しうる）を示す。Δd = |0 - 2| = 2。
```

**新:**
```
**Δd の根拠.** 忘却関手のカーネルの微分的階数で定義する (§2.3 Δd の統一的定義参照):
- ker(U_top) = 計量 g: 計量は2つの接ベクトルを消費してスカラーを返す双線形形式であり、**2階の微分構造**を持つ (Riemann 曲率テンソルは計量の2階微分から構成される)。ord = 2
- ker(U_met) = 位相: 位相多様体は開集合の族であり、微分構造を前提しない**0階構造**。ord = 0
- Δd = |2 - 0| = 2。真空解 $T_{\mu\nu} = 0$ の存在が CPS0'（0階構造は2階構造なしに存在しうる）を示す

**注意.** Sym²T* の「2」は計量が対称2階テンソルであることを表すが、Δd の根拠はテンソルの階数ではなく、計量が**定義する微分構造の階数** (ord = 2) である。同じ Sym²T* でも、その切断が定義する構造の複雑さが Δd を決める。
```

### §2.5.4 (心身問題) の Δd 根拠

**旧:**
```
**Δd の根拠.** Paper I の構造を直接使う:
- 身体的状態 θ ∈ Θ は状態空間の1点 = 0-cell
- 推論構造 ∇^(α) は統計多様体上の接続 = 1-cell（方向を与える: "あるもの"）
- 意識 F_{ij} = (α/2)[d(ΦT)]_{ij} は接続の曲率 = 2-cell（方向の変化を測る: "するもの"）
```

**新:**
```
**Δd の根拠.** 忘却関手のカーネルの微分的階数で定義する (§2.3 Δd の統一的定義参照):
- ker(U_body) = 推論構造 ∇^(α) + 曲率 F: 接続は**1階の微分構造** (勾配, 方向)、曲率は**2階の微分構造** (方向の変化)。ord = max(1, 2) = 2
- ker(U_mind) = 状態空間の座標 θ: 状態空間の点は**0階構造** (位置, 値)。ord = 0
- Δd = |2 - 0| = 2

**解釈の変更なし.** 「身体 = 0-cell, 心 = 1-cell, 意識 = 2-cell」の直感は保たれる。変更されたのは「cell」の正確な意味: テンソルの添字の数ではなく、忘却関手のカーネルが失う微分構造の階数。
```

---

## PATCH 4: §3.3.1 を命題から定義に格下げ [精読レポート P2-F3 の修正]

### 差替え

**旧:**
```
**命題 3.3.1 (α の層化).** Paper I の精度場 α(θ) と Paper II の非対称性パラメータ Δd は、以下の層化構造を成す:
```

**新:**
```
**定義 3.3.1 (α の層化).** Paper I の精度場 α(θ) と Paper II の非対称性パラメータ Δd を、以下の層化構造で関係づける:
```

### 「証明」→「構成の動機」に変更

**旧:**
```
*証明.* Paper I §6 の構成を逆から読む。(1) CPS ペアが与えられると...
...まとめると、Δd はグローバルな位相的分類であり α(θ) はそのファイバー内の局所的力学場である。 □
```

**新:**
```
*構成の動機.* この定義は Paper I §6 の構成を逆から読むことで動機づけられる。(1) CPS ペアが与えられると...
...まとめると、Δd はグローバルな位相的分類であり α(θ) はそのファイバー内の局所的力学場である。

**注意.** α(θ_∞) の整数部分が Δd に対応するという関係は、本稿では**定義**として採用する。Amari の α-接続族における α ∈ ℝ と CPS の cell 次元差 Δd ∈ ℕ の間の数学的な導出は今後の課題であり、現時点では上記の層化構造は motivated construction として位置づけられる。
```

### 系 3.3.2 は変更なし
系 3.3.2 (力の二重起源) は定義 3.3.1 からの帰結として書き直しても内容は同じ。「命題」→「定義」の変更は系の正当性に影響しない（系は定義から導出される構造的観察であり、命題の真偽には依存しない）。

---

## PATCH 5: §3.4 Face Lemma の全面差替え

### 差替え範囲
「### 3.4 Face Lemma:」から「直感的再述:」の末尾まで

### 新しい §3.4

```markdown
### 3.4 Face Lemma: CPS が非自明であるための最小構造

CPS の非対称性 (α > 1) が力を生むには、忘却の「方向」が測定可能でなければならない。これは忘却量 Θ (スカラー) から忘却スペクトラム Ξ (形状) への昇格を意味する。

#### 3.4.1 数学的準備: nerve と 2-coskeletal 性

圏 $\mathcal{C}$ の nerve $N(\mathcal{C})$ は単体的集合であり、$N_n$ は $n$ 個の合成可能な射の列からなる。Grothendieck の nerve 特性化定理 [23] により、圏の nerve では全ての**内部 horn** $\Lambda^n_k$ ($n \geq 2$, $0 < k < n$) が**一意に**充填される。

**系 (nerve の 2-coskeletal 性).** $n \geq 3$ の simplex は 2次元面から一意に復元される。これは全ての圏の nerve に対して成立し、Kan complex の仮定を必要としない。

*証明.* $n = 3$ の場合: 3-simplex の内部 horn は 2-simplex（面）の集合から構成され、Grothendieck 定理により一意に充填される。一般の $n$ に対しては帰納法。$\square$

#### 3.4.2 忘却スペクトラムの次元

**定義 (Star complex).** 対象 $x \in \text{Ob}(\mathcal{C})$ の star complex $\text{St}(x)$ は、$x$ を頂点として含む $N(\mathcal{C})$ の全 simplex の和。

**定義 (dim Ξ).** 対象 $x$ の忘却スペクトラムの次元:

$$\dim \Xi(x) := \max\{k \mid \text{St}(x) \text{ が非退化 } k\text{-simplex を含む}\}$$

ここで「非退化」は退化作用素の像でないこと（恒等射のみからなる simplex を除外）を意味する。

#### 3.4.3 CPS-安定性

**定義 (CPS-安定性).** CPS 圏の対象 $x$ が CPS-安定であるとは:

(S1) **非自明性**: $\text{St}(x)$ が恒等射以外の射を含む

(S2) **検証可能性**: $\text{St}(x)$ が非退化 2-simplex を含む

#### 3.4.4 Face Lemma

**定理 3.4.1 (Face Lemma).** $\mathcal{C}$ を圏、$x \in \text{Ob}(\mathcal{C})$ とする。

**(a) 最小性.** $x$ が CPS-安定であるための最小条件は $\dim \Xi(x) = 2$。

**(b) 等価条件.** 以下は同値:
(i) $\dim \Xi(x) \geq 2$
(ii) $x$ を頂点とする composable triple $(f, g, h)$ with $g \circ f = h$ が存在する（$f, g$ は恒等射でない）
(iii) $x$ は CPS-安定である

**(c) 飽和.** $\dim \Xi(x) \geq 3$ は $\dim \Xi(x) = 2$ に対して構造的に新しい情報を持たない（nerve の 2-coskeletal 性 [§3.4.1] による）。

*証明.*

**(a)** dim Ξ の各値で (S1), (S2) を検証する:

**dim Ξ = 0:** St(x) は $x$ 自身のみ。(S1) 不成立。

**dim Ξ = 1:** St(x) は 1-simplex（射）を含むが 2-simplex を含まない。(S1) 成立。(S2) 不成立: 2-simplex が存在しないため合成制約 $g \circ f = h$ が検証不能。忘却量 $\Theta$ はスカラーとして測定可能だが方向 $\partial_i \Theta$ は定義できない——方向の測定には 2-simplex の2つの独立な辺が提供する2軸が必要。

**dim Ξ = 2:** St(x) は非退化 2-simplex $(f, g, h)$ を含む。(S1): $f \neq \text{id}$ より成立。(S2): 2-simplex の存在により成立。$\square$

**(b)** (i)⟹(ii): dim Ξ ≥ 2 ならば非退化 2-simplex が存在し、その辺が composable triple。(ii)⟹(iii): composable triple が (S1), (S2) を充足。(iii)⟹(i): (S2) より dim Ξ ≥ 2。$\square$

**(c)** Nerve の 2-coskeletal 性 [§3.4.1] により、$n \geq 3$ の simplex は 2次元面から一意に復元される。新しい simplex は既存の 2-simplex の合成の「記録」にすぎず、構造的に新しい情報を持たない。$\square$

直感的再述:
- **2射** (1-skeleton): 表と裏。MECE だが冗長性ゼロ。合成 g∘f は f, g の派生であり外部検証不能。Θ (スカラー) しか測れず、**忘却の方向が見えない**
- **3射** (2-skeleton): 三角形。面積 = 冗長性 = 自由度。合成 g∘f = h が検証可能。Ξ (スペクトラム形状) が見え、**力の方向がわかる**
```

---

## PATCH 6: §3.5 系「三者の一致」の修正

### 差替え

**旧:**
```
$$\min|Hom|_{\text{verifiable}} = \min \dim(\Xi)_{\text{non-trivial}} = |\text{Fix}(G \circ F) \text{ attributes}| = 3$$

これらは全て |Δ_F| = 3 (三角恒等式の射の数) から導出される。
```

**新:**
```
| 量 | 値 | 由来 |
|:---|:---|:---|
| $\min|\text{Hom}|_{\text{verifiable}}$ | **3** (射の数) | 2-simplex の辺の数 = 3 |
| $\min \dim(\Xi)_{\text{non-trivial}}$ | **2** (simplex 次元) | 定義 3.4.2: 非退化 2-simplex の存在 |
| $|\text{Fix}(G \circ F) \text{ attributes}|$ | **3** (属性の数) | 不動点性 + 展開可能性 + 自己参照性 |

射の数 (3) と simplex の次元 (2) の差異は、2-simplex が 3辺を持つが 2次元であることに対応する。三角恒等式 $\Delta_F$ は 3射の合成制約であり、その制約が張る空間は 2次元 (3射 - 1制約 = 2自由度)。
```

---

## PATCH 7: §3.7.2 Blanket 生成定理の (i)⟹(ii) への CPS 経由条件の追加

### 差替え箇所
§3.7.2 の「**(i) ⟹ (ii).**」の直後

### 追加内容
「CPS スパンの忘却関手 U_A, U_B の方向性 (§2) に基づく自然な割り当て:」の前に以下を挿入:

```
Layer 1 条件 (5) (CPS 経由, §2.1) により、composable triple $(f, g, h)$ の射は CPS スパンの異なる脚に沿う:
```

### §3.7.2 の証明 (iii)⟹(i) の修正

**旧:**
```
**(iii) ⟹ (i).** dim Ξ ≥ 2 のとき、Face Lemma (§3.4) の証明の逆向き: dim Ξ = |独立な生成射| - 1 ≥ 2 より |独立な生成射| ≥ 3。3つの独立な生成射は composable triple を構成し、N(X) の 2-skeleton を与える。 □
```

**新:**
```
**(iii) ⟹ (i).** dim Ξ ≥ 2 のとき、定義 3.4.2 (§3.4) より St(X) に非退化 2-simplex が存在する。Face Lemma 定理 3.4.1 (b) の (i)⟹(ii) により composable triple が存在し、N(X) の 2-skeleton を与える。 □
```

---

## PATCH 8: 参照文献の追加 + バージョン表記

### 参照文献に追加
```
[23] Segal, G. (1968). Classifying spaces and spectral sequences. *Publications Mathématiques de l'IHÉS* 34, 105-112.
```

### バージョン表記の更新

**旧:**
```
*Paper II, 骨格 v1.1 — 2026-03-27*
```

**新:**
```
*Paper II, 骨格 v1.2 — 2026-03-27*
*変更: v1.2 — §3.4 Face Lemma を Grothendieck nerve 特性化定理に基づき全面改訂 (dim Ξ の循環的定義を除去, Kan complex 仮定を除去, CPS-安定性を形式定義)。§3.3.1 を命題→定義に格下げ。§2.3 Δd 割り当て基準を「忘却関手カーネルの微分的階数差」に統一。Layer 1 に第5条件 (CPS 経由) 追加。§3.5 三者の一致を修正 (3射 vs 2次元の区別)。文献 [23] 追加*
```

---

## 適用手順

1. PATCH 2 (§2.3 Δd 統一) を先に適用 — 後続の PATCH 3 が参照する
2. PATCH 1 (Layer 1 第5条件) を適用
3. PATCH 3 (§2.5.3, §2.5.4 修正) を適用
4. PATCH 4 (§3.3.1 格下げ) を適用
5. PATCH 5 (§3.4 全面差替え) を適用
6. PATCH 6 (§3.5 三者の一致) を適用
7. PATCH 7 (§3.7.2 修正) を適用
8. PATCH 8 (文献 + バージョン) を適用

### 全パッチ適用後の整合性チェック

- [ ] 「4条件」→「5条件」の文言が全箇所で更新されたか
- [ ] §3.4 の旧証明テキストが完全に除去されたか
- [ ] §3.7.2, §6.1, §6.2 の Face Lemma 参照が定理 3.4.1 を指しているか
- [ ] §2.5 の各インスタンスの「Face Lemma 具体化」が dim Ξ の新定義と整合するか
- [ ] 文献 [23] が本文中で引用されているか (§3.4.1)

---

*パッチ文書完了: 2026-03-27*
*修正箇所: 8 patches, 影響範囲: §2.1, §2.3, §2.5.3, §2.5.4, §3.3.1, §3.4, §3.5, §3.7.2, 参照文献*
