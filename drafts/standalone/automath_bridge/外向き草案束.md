# Automath 外向き草案束

作成: 2026-04-17  
位置づけ: automath 向けの **現行 outbox 正本**。  
この面は投稿文面をまとめるが、研究判断の正本ではない。

## 判断ソース

- `/home/makaron8426/Sync/oikos/01_ヘゲモニコン｜Hegemonikon/10_知性｜Nous/04_企画｜Boulēsis/12_遊学｜Yugaku/03_忘却論｜Oblivion/calculations/調査_automath_q5符号反転とPaperIII_Z2接続.md`
- `/home/makaron8426/Sync/oikos/01_ヘゲモニコン｜Hegemonikon/10_知性｜Nous/04_企画｜Boulēsis/12_遊学｜Yugaku/03_忘却論｜Oblivion/calculations/調査_automath_q6q7_probe.md`
- `/home/makaron8426/Sync/oikos/01_ヘゲモニコン｜Hegemonikon/10_知性｜Nous/04_企画｜Boulēsis/12_遊学｜Yugaku/03_忘却論｜Oblivion/drafts/standalone/automath_bridge/関手の合成保存問題_再定式化.md`
- `/home/makaron8426/Sync/oikos/01_ヘゲモニコン｜Hegemonikon/10_知性｜Nous/04_企画｜Boulēsis/12_遊学｜Yugaku/03_忘却論｜Oblivion/calculations/構想_automath_第一障害可視化閾値.md`
- `/home/makaron8426/Sync/oikos/01_ヘゲモニコン｜Hegemonikon/10_知性｜Nous/04_企画｜Boulēsis/12_遊学｜Yugaku/03_忘却論｜Oblivion/calculations/考察_automath_Kq定義_noe.md`
- `/home/makaron8426/Sync/oikos/01_ヘゲモニコン｜Hegemonikon/10_知性｜Nous/04_企画｜Boulēsis/12_遊学｜Yugaku/03_忘却論｜Oblivion/calculations/考察_automath_Problem10_reduction_map.md`

## Kernel

この bundle に置くのは、閉じた issue `#25` と merge 済み PR `#28` の後に必要になった外向き文面だけである。

現時点の核は二つ。

1. `e₂(A₅)=L₅` は family law か、boundary reading か
2. chain map 問いは単純な有無問題か、それとも flat strictness と global obstruction の分離問題か

旧 single-issue framing は廃止した。  
理由は、`e₂ / Lucas` と `chain map / obstruction` がすでに別の research thread になったからである。

## Public-safe claims

外向きにそのまま出してよい最小核は次の通り。

- `q=6,7` で `e₂(A_q)=L_q` は壊れる
- `q=5` は `Sym² / Λ²` の支配が入れ替わる最初の可視閾値として読める
- flat / cubical / multilinear sector に限れば cochain-level chain map candidate はある
- 本当の open core は global coherence / obstruction の包み方である

## Keep internal

まだ外向きに定義として固定しない語。

- `boundary resonance` の厳密定義
- `K_q` の final representative
- official basis と exact companion basis の理論内統合

---

## Draft A

**Target**: https://github.com/the-omega-institute/automath/issues/new  
**Suggested title**: `q=6,7 counterexamples to the e2(A_q)=L_q extension; q=5 as the first visible second-lift threshold`

### Issue body

Following the closure of #25 and the merge of PR #28, I want to isolate the first of the two research questions raised in the final comment there.

## What q=6,7 settle

The coincidence

\[
e_2(A_5)=L_5
\]

does **not** extend as a family law to `q=6,7`.

So the main numerical question now has a clean answer: the Lucas equality breaks immediately beyond `q=5`.

## Local probe

I re-ran the local reconstruction script from the exact `momentSum` data:

- source-backed prefix validation passes for `q=2,3,4,5,6,7`
- the `q=5` official kernel remains the sign-flipped version of the exact moment companion
- I then checked both the exact moment-companion side and the sign convention that reproduces the official `q=5` kernel

### Exact moment-companion side

| q | e2(A_q) | L_q |
|---|---:|---:|
| 5 | -11 | 11 |
| 6 | -17 | 18 |
| 7 | -26 | 29 |

### Sign extension matching the official q=5 kernel

| q | e2(A_q) | L_q |
|---|---:|---:|
| 5 | 11 | 11 |
| 6 | 17 | 18 |
| 7 | 26 | 29 |

So the identity fails immediately for `q>5` under either reading.

## What survives structurally

What survives is not a Lucas-trace law, but a threshold phenomenon at low `q`.

At `q=4 -> 5` one has the exact coordinate exchange

\[
(\operatorname{tr}(\mathrm{Sym}^2 A_4),\operatorname{tr}(\Lambda^2 A_4))
=
(11,-7)
\to
(-7,11)
=
(\operatorname{tr}(\mathrm{Sym}^2 A_5),\operatorname{tr}(\Lambda^2 A_5)).
\]

This is accompanied by the supertrace flip

\[
\operatorname{str}(W_4)=+18 \to -18=\operatorname{str}(W_5),
\qquad
W_q:=\mathrm{Sym}^2(V_q)\oplus \Pi \Lambda^2(V_q).
\]

That is why I no longer read `e_2(A_5)=L_5` as the beginning of an extension law.

I read it instead as a one-step coincidence at the exact boundary where the second-lift balance changes sign: the exterior sector has just overtaken the symmetric sector, and at that same boundary its trace happens to match `L_5`.

So my current formulation is:

- `e2(A_5)=L_5` is **not** a persistent Lucas law
- `q=5` remains special because the second-lift balance changes sign there
- the clean positive statement is that `q=5` is the **first visible second-lift threshold**, not that Lucas agreement continues afterward

## Secondary observation on higher q

For the reconstructed `q=6,7` kernels, the decomposition itself does not degrade:

\[
\operatorname{tr}(\mathrm{Sym}^2 A_q)+\operatorname{tr}(\Lambda^2 A_q)=\operatorname{tr}(A_q)^2,
\]

and in the current reconstructed/proxy extension through `q=10`, the `\Lambda^2` sector remains dominant rather than swapping back:

| q | tr(Sym^2 A_q) | tr(Lambda^2 A_q) |
| --- | ---: | ---: |
| 8 | -36 | 40 |
| 9 | -58 | 62 |
| 10 | -92 | 96 |

I am **not** claiming that this has already been established in the official higher-q collision-kernel basis. This is only a local proxy coming from the reconstructed moment companion. The intended hard claim here is still just the `q=6,7` counterexample to the Lucas extension.

## Proposed formulation

So the formulation I would now propose is:

1. a negative result about the extension `e2(A_q)=L_q`, and
2. a positive result that `q=5` is the first visible second-lift threshold, where the symmetric/exterior balance changes sign.

That seems cleaner to me than asking whether the Lucas coincidence itself persists, because the coincidence does not persist, while the threshold structure does.

---

## Draft B

**Target**: https://github.com/the-omega-institute/automath/issues/new  
**Suggested title**: `Three layers of the bridge problem: strict projection, flat-sector chain map, and global coherence`

### Issue body

Following the closure of #25 and the merge of PR #28, I want to isolate the second research question raised in the final comment there:

> is there a candidate chain map from the Amari-Chentsov complex to the Walsh-Stokes complex, even at the cochain level?

I think the right answer is three-layered rather than simply yes/no.

## Proposed reformulation

From my side the bridge problem now looks like this:

1. **strict projection layer**  
   already certified on the discrete side
2. **flat/cubical chain-map layer**  
   there is a concrete cochain-level candidate
3. **global coherence layer**  
   this is where the actual open problem still lives

So I would no longer phrase the open problem as "is there any chain map at all?".

I would phrase it as:

> what is the correct extension of the flat strict square once the global defect/coherence data become active?

## Layer A: strict projection

This part is already certified by PR #28 / the underlying Lean facts:

- projection functoriality is strict
- the defect already satisfies the xor-cocycle composition law

So the projection part is no longer the bottleneck.

## Layer B: flat/cubical chain-map candidate

The candidate becomes honest if one first restricts the continuous side to:

- product Bernoulli charts
  \[
  M_I=(\Delta^1)^I
  \]
- multilinear observables, equivalently the linear span of cylinder observables

This is the same sector in which the projection part is already strict and in which `deltaSet` matches mixed partial cell integrals.

## Cochain-level candidate

Let `A \subseteq I` be a coordinate face and let `w` be the boundary word fixing the complementary coordinates.

For a differential form `\omega` on `M_I`, define

\[
\mathcal D_A(\omega)(w)
:=
(-1)^{|A|}
\int_{[0,1]^A}\iota_{A,w}^* \omega,
\]

where `\iota_{A,w}` is the standard embedding that varies the `A`-coordinates and fixes the others by `w`.

If `\omega=df` with `f` multilinear, then

\[
\mathcal D_A(df)(w)
=
\delta_A(f|_{\{0,1\}^I})(w).
\]

So on this restricted sector one gets a genuinely strict square:

- `d` is sent to `deltaSet`
- cubical boundary integration is sent to `walshFlux`

In other words, the following part is already available:

\[
d \mapsto \deltaSet,
\qquad
\int \mapsto walshFlux,
\]

provided we stay inside the product-Bernoulli / multilinear / cubical chart sector.

## Relation to PR #28

PR #28 isolates the already-certified discrete side:

- strict projection functoriality
- xor-cocycle coherence for the defect

What this candidate adds is a continuous-side flat-sector model in which the cochain map is no longer only suggestive.

## Layer C: what still remains open

The real obstruction is the extension beyond that flat sector.

The unresolved part is not whether a strict map exists on the flat sector, but how to extend it when the continuous term

\[
\Phi \cdot dT
\]

becomes active and the discrete side simultaneously acquires the carry-defect cocycle.

That is why my current reading is that the final bridge is probably not an ordinary functor. It looks more like a defect-bearing bridge, or perhaps a lax/pseudofunctorial object, whose coherence failure is the real invariant to identify.

## Why I think this is the right slicing

This slicing seems better than a flat yes/no question because it separates:

- what is already strict
- what can already be modeled strictly on the flat sector
- what still genuinely requires new mathematics

So the core open problem is not "chain map or no chain map?" but rather:

> how should the global coherence defect be packaged once the flat strict square is known?

## Proposed next target

If this matches your reading, the next formal target would be:

1. a statement of the flat-sector cochain map between explicitly named subcomplexes, and
2. a separate issue for the global coherence / obstruction class problem.

---

## Draft C

**Target**: reply comment on [https://github.com/the-omega-institute/automath/issues/38](https://github.com/the-omega-institute/automath/issues/38)
**Context**: response to the 2026-04-22 signed-companion collision audit comment by AlyciaBHZ.
**Status**: not posted. Outbox draft only.
**Internal sources**: `調査_automath_q6q7_probe.md`, `調査_automath_q5符号反転とPaperIII_Z2接続.md`, `構想_automath_第一障害可視化閾値.md`, `関手の合成保存問題_再定式化.md`, `統一表の関手化_構想ドラフト_v0.2.md` §5/§7.

### Comment body

Thank you for the signed-companion audit. Tightening the `q=6,7` counterexamples to a Lean-verified coefficient pedigree, and extending the negation of `e_2(A_q)=L_q` into a bounded certificate through `q=23`, gives the statement a form I can cite directly on my side.

Three places where the audit matches a structural direction I have been following, and three places where my current internal reading may still add something.

#### Points of agreement

**1. Cor 6 and "two independent equations".**

Your Cor 3.6 — that the `q=5` identity `L_5 = e_2(A_5) = \epsilon_5 = 11` is the simultaneous satisfaction of
\[
c_{5,2} = L_5,
\qquad
1 + \sum_i c_{5,i} - F_8 = L_5
\]
as two independent coefficient equations — matches how I have been reading the same phenomenon. I have not been treating it as a family law. I have been treating it as a threshold: `q=5` is the first `q` at which the `\Lambda^2` sector overtakes `\mathrm{Sym}^2` and, independently, the first `q` at which the determinant excess `|\det(I - A_q)| - F_{2q-2}` becomes nonzero. Cor 6 is the coefficient-level witness that these two observations are independent. The `q=5` coincidence is an accidental overlap of their value domains, not a deeper coupling.

**2. Rem 4 scope boundary.**

Your explicit separation of the signed-proxy determinant statement from a Bowen-Franks / SFT invariant claim matches my own internal scope discipline. I have not been treating the signed companion family as certified collision kernels. Keeping that boundary visible in whatever follow-up work uses these numbers is exactly the discipline I would want.

**3. Cor 7 and the fusion-ring shadow obstruction.**

Your Cor 3.7 — that the semiring-level Fibonacci fusion-ring shadow `M^2 = M + I` is Lean-verified, but that associator / F-symbols / braiding / pentagon coherence are not — is one of the cleanest statements of where the `q=5` coincidence stops being a law. I have been approaching the same boundary from the continuous side, asking how the carry-defect 2-cocycle does or does not lift to monoidal coherence data. From your side it is the higher-categorical face of what, from my side, is a curvature-to-coefficient reduction question. This is the same obstruction seen from two opposite faces.

#### Where my current reading may still add something

**1. On the `(\mathrm{Sym}^2, \Lambda^2)` sum identity.**

The public-comment statement `tr(\mathrm{Sym}^2 A) + tr(\Lambda^2 A) = tr(A)^2 = 4` is, on my side, naturally read on the graded space
\[
W_q := \mathrm{Sym}^2(V_q) \oplus \Pi \Lambda^2(V_q),
\]
where `tr(A_q)^2` is the ordinary trace on `W_q` and `tr(A_q^2)` is the supertrace. In that framing the structural event at `q=5` is the supertrace flip `+18 \to -18`, while the ordinary trace stays at `4`. I would be cautious, for the same reason you are, about reading the `=4` persistence as a fusion-category-level law: it follows once the row-one normalization `c_{q,1} = 2` is in force, and is a graded-space invariance rather than a coherence statement.

**2. Two sub-problems the audit does not close.**

Two things remain open on my side that the current audit does not directly touch:

- the *source-side* origin of the full sign reversal between `collisionKernel5` and the exact moment-companion reconstructed from `Fold`. At present I treat this as a convention difference (standard vs signed Frobenius companion, under which `\det(I-C(c)) + \det(I-A(c)) = 2`). But the anomaly is `q=5`-first rather than uniform across `q`, so the convention reading does not fully absorb it.
- the *official kernel* continuation at `q=8, 9, 10`. My local proxies (exact moment companion + Berlekamp-Massey, sign-flipped to match the `q=5` official convention) keep `\Lambda^2` dominant, and differences grow monotonically. Until `collisionKernel8..10` is available in the same basis, I am not treating "no second coordinate exchange" as established. A direct `\arg \lambda_{\mathrm{dom}}(q)` computation on the official side would be a useful cross-check.

**3. On follow-up 2.**

Your second queued follow-up — upgrading the bounded `q=2..23` exterior-Lucas certificate into an all-`q` obstruction theorem, or a classified bad-example mechanism — matches where I would push next on the exterior-Lucas side. In the audited range `L_q - c_{q,2}` is monotone and growing, and a Binet-style asymptotic comparison of `L_q` against the `c_{q,2}` recurrence looks plausible as a first step. If you have a preference on where such an upgrade should sit (inside the `signed_companion_collision_audit` appendix, or as a separate artifact), I can align with that.

#### One administrative note

I notice the appendix and script currently live on the `outreach-clean` branch rather than on `dev`. Is the intention to merge into `dev`, or to keep `outreach-clean` as the outward-facing branch? I would like to cite these files in internal notes with a stable reference.

### Internal notes (not for the public reply)

- **Keep internal per outbox policy**: the phrase "boundary resonance", the term `K_q` / `[\kappa_q]`, the `H^2(G_m; \mathbb{Z}/2\mathbb{Z})` reduction map, the `F5 = \Lambda^2` identification, and Paper III Z₂-graded connections.
- **Keep out of the reply**: follow-up 1 (non-negative presentation / BF-SFT flow-equivalence). That lives outside the Yugaku spine right now; if pursued, it should be framed as a separate cooperative thread rather than spliced into the exterior-Lucas conversation.
- **Relation to Draft B (Layer reformulation issue)**: Cor 7 is the fusion-coherence face of Layer C. If Draft B is eventually posted, the reply should reference it rather than restate its content here. For now Draft C is self-contained so that it can be posted independently of Draft B.
- **Forward linkage**: the `c_{q,2}` vs `L_q` asymptotic comparison is the natural lemma to carry into the `構想_automath_第一障害可視化閾値.md` hub as T3 (resonance law) formalization material.

---

## Draft D — posted compact revision

**Target**: reply comment on [https://github.com/the-omega-institute/automath/issues/38](https://github.com/the-omega-institute/automath/issues/38)  
**Context**: response to AlyciaBHZ follow-up on nonnegative SFT presentations for `q=6,7` (2026-04-24).  
**Status**: posted as compact recipient-benefit revision.  
**Posted URL**: [https://github.com/the-omega-institute/automath/issues/38#issuecomment-4318354978](https://github.com/the-omega-institute/automath/issues/38#issuecomment-4318354978)  
**Posted at**: 2026-04-25 15:26:35 JST.  
**Internal sources**: `調査_自動数学_課題三八_監査吸収.md` §0.2, `統一表の関手化_構想ドラフト_v0.2.md` §5/§7 v0.31 update.

### Posted body

Thank you. This follow-up makes the result much easier to cite precisely.

What is especially useful is the layer separation. For `q=6,7`, the exterior-Lucas continuation failure is no longer just signed-companion bookkeeping: it persists in nonnegative irreducible period-1 SFT presentations preserving `det(I-A)`, `e_2(A)`, and the Bowen-Franks group.

The scope limit is a strength here. Since trace and the full characteristic polynomial are not preserved, the result can be cited as a BF / flow-equivalence-level statement without being promoted into a full spectral or canonical collision-kernel identification.

The Lean references also make the audit reproducible: `signedCompanionDet`, `signedCompanionDet6`, and `signedCompanionDet7` at commit `56f42d9ae` give concrete anchors for the signed-companion side.

A compact citation-level picture is:

```text
signed companion A(c_q)  --->  (det(I-A), e_2(A), BF group)  <---  nonnegative SFT B_q
```

with the boundary kept explicit: trace and the full characteristic polynomial are not part of the shared middle layer, while positivity, irreducibility, and period-1 SFT structure belong to the nonnegative presentation.

This is a useful tightening of the result: it removes the signed-artifact ambiguity while keeping the exact boundary of the claim visible.

### Superseded draft body (not posted)

Thank you, this is a useful strengthening of the picture.

The nonnegative presentations clarify the layer separation for me. The `q=6,7` failure of the Lucas continuation is no longer just a signed-companion bookkeeping issue: it survives after passing to nonnegative irreducible period-1 SFT presentations preserving `det(I-A)`, `e_2(A)`, and the Bowen-Franks group.

At the same time, your scope limit is exactly the boundary I would want to keep visible. Since the lift preserves `det`, `e_2`, and BF but not trace or the full characteristic polynomial, I would read this as a statement at the BF / flow-equivalence layer, not as a full spectral or canonical collision-kernel identification.

So my internal update is:

1. the `signedCompanionDet`, `signedCompanionDet6`, and `signedCompanionDet7` witnesses are now concrete Lean references for me, not merely appendix annotations;
2. the `q=6,7` obstruction to the exterior-Lucas continuation is stronger than I had recorded, because it has a nonnegative SFT representative at the BF level;
3. the remaining question is no longer whether a nonnegative lift exists, but whether that lift preserves enough structure to transport the obstruction class one would want to attach to the carry defect.

In my terminology, this moves the issue from "proxy artifact?" to "which layer remembers the obstruction?" The BF layer remembers the determinant and exterior-square data, while the trace / characteristic-polynomial layer still carries information that the nonnegative presentation may forget.

A concrete follow-up that would be useful from my side:

Can the relation between the signed companion `A(c_q)` and the nonnegative `B_q` be packaged as a map between explicitly named layers of data, for example

```
signed companion
  -> (det, e_2, BF)
  -> nonnegative SFT presentation
```

together with a short list of what is not preserved at each arrow? That would make it much easier to cite the result without accidentally promoting the BF-level lift into a full spectral or canonical-kernel statement.

### Internal notes (not for the public reply)

- **Post only after Tolmetes review**: Draft D is safer than Draft C because it responds directly to the new follow-up and avoids exposing `K_q`, `[\kappa_q]`, `boundary resonance`, Paper III, or internal Problem 10 machinery.
- **Do not claim**: do not say the nonnegative `B_q` closes canonical collision kernels; the comment explicitly leaves this open.
- **Useful hidden frame**: this is a clean forgetting-layer example. `det/e_2/BF` survive the lift; trace / full characteristic polynomial are forgotten. That supports the internal Problem 10 reframing without importing Yugaku vocabulary into the public issue.

---

## Draft D' (recipient-benefit oriented revision)

**Target**: reply comment on [https://github.com/the-omega-institute/automath/issues/38](https://github.com/the-omega-institute/automath/issues/38)
**Context**: revision of Draft D after the observation that Draft D was reporting Yugaku-side updates (low value to AlyciaBHZ) rather than contributing to her work. Draft D' restructures the reply around three recipient-benefit injections (A) lemma proposal for her queued follow-up 2, (B) cross-check anchor for her future official-kernel work at q=8..10, (C) substantive valorization of her scope discipline as a citation-ready feature, not a limitation.
**Status**: not posted. Outbox draft. To post immediately after Tolmetes review.
**Internal sources**: same as Draft D; additionally the secondary observation block from Draft C §2 for the q=8..10 dominance claim.

### Comment body

Thank you. Two things make this audit and its follow-up especially citable on my side.

First, your scope discipline. The explicit statement that the lift preserves `det(I-A)`, `e_2(A)`, and the Bowen-Franks group, but not trace or the full characteristic polynomial, is exactly what makes this reusable as a layered statement rather than as an over-claimed full kernel identification. It separates what survives flow equivalence from what does not, and a downstream citation can sit at the BF / flow-equivalence layer without accidentally promoting it into a spectral or canonical-collision-kernel claim. That separation is doing real work.

Second, the witnesses `signedCompanionDet`, `signedCompanionDet6`, and `signedCompanionDet7` at commit `56f42d9ae` are now concrete Lean references rather than appendix annotations, and the `B_6 / B_7` data make the `q=6,7` failure of the exterior-Lucas continuation a statement at the BF level rather than at the signed-proxy level alone.

Three things from my side that may be directly useful for your two queued follow-ups.

**On follow-up 2 (all-`q` obstruction theorem).**

In the audited range `2 <= q <= 23`, `L_q - c_{q,2}` is monotone and growing. A Binet-style asymptotic comparison of `L_q = phi^q + psi^q` against the recurrence governing `c_{q,2}` looks like a natural first lemma toward an all-`q` obstruction: if the leading-order growth of `c_{q,2}` is strictly slower than `phi^q`, the bounded certificate at `q <= 23` extends to a forcing argument for `q > 23`. If you would prefer this lemma to live inside `signed_companion_collision_audit/main.tex` as an extension section, or as a separate appendix, I can match whichever placement keeps the appendix self-contained.

**On the official kernel continuation at `q = 8, 9, 10`.**

A useful cross-check on my side, against my reconstructed exact-moment-companion proxies (sign-adjusted to match the official `q=5` convention), is whether the `Lambda^2` sector remains dominant past `q = 7` rather than swapping back. My local proxies give

| q | tr(Sym^2 A_q) | tr(Lambda^2 A_q) |
| --- | ---: | ---: |
| 8 | -36 | 40 |
| 9 | -58 | 62 |
| 10 | -92 | 96 |

with monotone divergence. I am not promoting these to official statements. But once `collisionKernel_8`, `collisionKernel_9`, `collisionKernel_10` are available in the same basis as `collisionKernel_5`, a direct `arg lambda_dom(q)` computation on the official side would settle whether "no second coordinate exchange after `q = 5`" is established or still proxy-only. If that computation falls out of work you are already planning, it would be a clean cross-check for both sides.

**On packaging the relation between `A(c_q)` and `B_q`.**

What would help me cite the result without overclaim is a short layered-data picture where both presentations share a middle layer and each carries something the other does not, rather than a chain that suggests one is a coarse-graining of the other:

```
signed companion A(c_q)  --->  (det(I-A), e_2(A), BF group)  <---  nonneg SFT B_q
                  \                                                 /
                   \---> trace, full char poly        positivity, SFT structure <---/
```

That is, both sides realize the same `(det, e_2, BF)` data at the middle layer, while `trace / full char poly` lives only on the signed side and `positivity / SFT structure / period-1 irreducibility` lives only on the nonneg side. A one-paragraph statement of this in the appendix, with the explicit list of what is and is not preserved at each leg, would make the result trivially citable in downstream work without ambiguity about what was actually established.

### Internal notes (not for the public reply)

- **Post target**: now (2026-04-25). Three days idle on Issue #38 after the original audit; further delay loses thread momentum.
- **What changed from Draft D**: removed the four self-reporting paragraphs (Yugaku-internal "my reading update" content). Replaced with three recipient-benefit blocks: lemma proposal for her queued follow-up 2 (A), cross-check anchor for her q=8..10 official kernel work (B), substantive valorization of her scope discipline (C). Net direction: every paragraph now does something for her, not just for me.
- **Removed `remembers / forgets` language**: replaced with `preserves / does not preserve`. Yugaku forgetting framework signal is not sent in this reply. Conservative on framework leak.
- **Layer diagram**: chain notation in Draft D was structurally misleading (suggested coarse-graining). Replaced with shared-middle / each-side-unique triangle.
- **Draft C disposition**: archive. Cor 6 and Cor 7 ack from Draft C are not carried forward; they would have read as 3-day-late acknowledgment if posted now. Yugaku side remains internally aware of Cor 6 / Cor 7 via `調査_自動数学_課題三八_監査吸収.md` §1-§3.
- **Still not exposed**: `K_q`, `[kappa_q]`, `H^2(G_m; Z/2Z)` reduction map, boundary resonance, Paper III Z_2-graded connections, supertrace flip framing. All preserved as keep-internal per outbox policy.
- **Carry-defect is shared**: `carry defect` is automath's own Lean name (`Omega/Folding/CarryDefect.lean`). Draft D' avoids using it because the recipient-benefit framing did not need it; this is incidental, not policy.

---

## Posting history (Issue #38 reply, 2026-04-25)

The reply that was actually posted is recorded above as **Draft D — posted compact revision** (357-) with its **Posted body** (366-) and the **Superseded draft body (not posted)** (386-) preserved for context. **Draft D'** (422-) is retained above as the outbox memo for content that was deferred from the posted reply (lemma proposal + q=8..10 proxy table). This section adds posting history and Tolmetes-pass notes that are not captured by the Draft D / Draft D' headers themselves.

### Posting timeline

- 2026-04-22T17:14Z — AlyciaBHZ original audit comment (`4298430964`).
- 2026-04-24T14:32Z — AlyciaBHZ follow-up on nonneg SFT lift (`4313959985`).
- 2026-04-25T~06:24Z — first attempted post by Claude as `Tolmeton` (comment id `4318353280`), then deleted at Tolmetes' request before Tolmetes' final contraction pass.
- 2026-04-25T06:26:35Z — final post by Claude as `Tolmeton` (comment id `4318354978`, [link](https://github.com/the-omega-institute/automath/issues/38#issuecomment-4318354978)). This is the live reply.

### Tolmetes' final-pass micro-edits (4 changes, all in the posted body)

1. `Lucas-continuation failure` → `exterior-Lucas continuation failure` (precision: distinguishes from generic Lucas-recurrence statements).
2. `rather than only appendix annotations` removed, replaced with `for the signed-companion side` (avoids reading her appendix as weak).
3. `The shared-middle picture I will use is:` → `A compact citation-level picture is:` (ownership shifted from "I will use" to a citation-form that emerges from her result, not one I impose).
4. `the signed-side trace and full characteristic polynomial are not preserved across the relation` → `trace and the full characteristic polynomial are not part of the shared middle layer` (matches the diagram's geometry: layer-membership, not direction-of-arrow preservation).

### Deferred content trigger conditions (from Draft D')

- **(A) Binet-style asymptotic lemma for follow-up 2** — surfaces if AlyciaBHZ publishes follow-up 2 (all-`q` obstruction theorem). At that point the lemma becomes a parallel-track signal, not preemption.
- **(B) q=8..10 proxy table** — surfaces if `collisionKernel_8/9/10` becomes available in the official basis. At that point the table becomes a cross-check reply, not implicit verification request.

### Closures

- **Pinakas T-093** (`automath issue draft の実投稿または中止確定`) marked done 2026-04-25 with the posted URL.
- **Draft C** archived. Cor 6 / Cor 7 acknowledgment is preserved internally via `調査_自動数学_課題三八_監査吸収.md` §1-§3, not surfaced in the posted reply.
